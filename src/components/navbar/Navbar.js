import React from "react";
import { SecondaryNavbar, MainNavbar } from "../index";

const Navbar = () => {
  return (
    <div>
      <SecondaryNavbar />
      <MainNavbar />
    </div>
  );
};

export default Navbar;
